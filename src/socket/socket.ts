/**
 * Socket.IO placeholder.
 * Connect to the existing Tasty Bites backend Socket.IO server later.
 * Do not invent event names or connection logic yet.
 */
export const socket = {
  isConnected: false,
  connect: (): void => {
    // TODO: connect using the existing backend Socket.IO infrastructure
  },
  disconnect: (): void => {
    // TODO: disconnect cleanly
  },
};
